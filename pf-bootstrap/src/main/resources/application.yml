spring.application.name: please-fridge
spring.profiles.active: local

spring:
  config:
    import:
      - db-core.yml
      - logging.yml
      - monitoring.yml
  web.resources.add-mappings: false

---
spring.config.activate.on-profile: local

---
spring.config.activate.on-profile: dev
